---
import styles from "./hero.module.scss";

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<div class={styles.hero} data-title={title}>
	<h1 class="clash-display container">{title}</h1>
</div>
